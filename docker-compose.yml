version: '3.8'

services:
  web:
    build: .
    container_name: snippet-vault-web
    restart: always
    ports:
      - "1234:1234"
    env_file:
      - .env
    environment:
      SQLITE_DB_PATH: /app/snippets.db
    volumes:
      - ./snippets.db:/app/snippets.db
